sudo docker build -t image_name .

sudo docker run -v folder_in_system:folder_in_container -p 5000:5000 -it image_name

sudo images

sudo ps -all

sudo rm image_name

sudo rmi container_id

sudo stats container_id

sudo start -i container_id
